import { useState } from "react";import { AlertCircle,CheckCircle2,Clock3,Inbox,Loader2,Plus,Search,Ticket as TicketIcon,UserCheck } from "lucide-react";import { useNavigate } from "react-router";import DashboardLayout from "@/components/layout/dashboard-layout";import { Input } from "@/components/ui/input";import { Textarea } from "@/components/ui/textarea";import { routesPath } from "@/routes/routes-path";import { cn } from "@/lib/utils";import { useCreateTicketMutation,useGetTicketDashboardQuery,useGetTicketsQuery,type TicketPriority,type TicketStatus } from "@/redux/services/tickets-api";
const statusClass:Record<TicketStatus,string>={OPEN:"bg-blue-50 text-blue-700",ASSIGNED:"bg-violet-50 text-violet-700",IN_PROGRESS:"bg-amber-50 text-amber-700",RESOLVED:"bg-green-50 text-green-700",CLOSED:"bg-gray-100 text-gray-600"};
export default function Support(){const nav=useNavigate();const dash=useGetTicketDashboardQuery();const[q,setQ]=useState("");const[status,setStatus]=useState("");const[page,setPage]=useState(1);const[creating,setCreating]=useState(false);const list=useGetTicketsQuery({page,q,...(status?{status}:{})});const d=dash.data?.data;const cards=[{label:"Total tickets",value:d?.total,icon:TicketIcon},{label:"Open",value:d?.by_status.OPEN,icon:Inbox},{label:"In progress",value:d?.by_status.IN_PROGRESS,icon:Clock3},{label:"Urgent",value:d?.by_priority.URGENT,icon:AlertCircle},{label:"Assigned to me",value:d?.assigned_to_me,icon:UserCheck},{label:"Resolved",value:d?.by_status.RESOLVED,icon:CheckCircle2}];return <DashboardLayout title="Support"><main className="px-4.5 py-6 lg:px-8"><div className="mx-auto max-w-7xl space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-2xl font-semibold">Support centre</h1><p className="mt-1 text-sm text-gray-01">Track requests, collaborate with support and resolve issues faster.</p></div><button onClick={()=>setCreating(true)} className="inline-flex items-center justify-center gap-2 rounded-md bg-primary px-4 py-2.5 text-sm font-semibold text-white"><Plus className="size-4"/>Create ticket</button></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6">{cards.map(c=><div className="rounded-xl border bg-white p-4 shadow-sm" key={c.label}><c.icon className="size-5 text-primary"/><p className="mt-4 text-2xl font-semibold">{c.value??"—"}</p><p className="text-xs text-gray-01">{c.label}</p></div>)}</div><section className="overflow-hidden rounded-xl border bg-white shadow-sm"><div className="flex flex-col gap-3 border-b p-4 md:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-2.5 size-4 text-gray-01"/><Input className="pl-9" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search number, title or description"/></div><select value={status} onChange={e=>setStatus(e.target.value)} className="h-9 rounded-md border bg-white px-3 text-sm"><option value="">All statuses</option>{["OPEN","ASSIGNED","IN_PROGRESS","RESOLVED","CLOSED"].map(s=><option key={s}>{s}</option>)}</select></div>{list.isLoading?<div className="grid h-60 place-content-center"><Loader2 className="animate-spin text-primary"/></div>:<div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-white-05 text-left text-xs text-gray-01"><tr><th className="p-3">Ticket</th><th>Requester</th><th>Category</th><th>Priority</th><th>Status</th><th>Assignee</th><th>Updated</th></tr></thead><tbody className="divide-y">{(list.data?.data??[]).map(t=><tr key={t.id} onClick={()=>nav(routesPath.PROTECTED.SUPPORT.DETAIL(t.id))} className="cursor-pointer hover:bg-gray-50"><td className="p-3"><p className="font-medium">{t.title}</p><p className="text-xs text-gray-01">{t.ticket_number}</p></td><td>{t.requester.name}</td><td className="capitalize">{t.category.toLowerCase()}</td><td><span className={cn("font-medium",t.priority==="URGENT"&&"text-red-600",t.priority==="HIGH"&&"text-amber-600")}>{t.priority}</span></td><td><span className={cn("rounded-full px-2 py-1 text-xs",statusClass[t.status])}>{t.status.replace("_"," ")}</span></td><td>{t.assignee?.name??"Unassigned"}</td><td className="pr-3 text-xs text-gray-01">{new Date(t.updated_at).toLocaleDateString()}</td></tr>)}</tbody></table>{!list.data?.data.length&&<div className="py-16 text-center text-sm text-gray-01">No tickets match these filters.</div>}</div>}{(list.data?.pagination.totalPages??0)>1&&<div className="flex justify-between border-t p-3 text-sm"><span>Page {page} of {list.data?.pagination.totalPages}</span><div className="flex gap-2"><button disabled={page===1} onClick={()=>setPage(x=>x-1)} className="rounded border px-3 py-1 disabled:opacity-40">Previous</button><button disabled={page===list.data?.pagination.totalPages} onClick={()=>setPage(x=>x+1)} className="rounded border px-3 py-1 disabled:opacity-40">Next</button></div></div>}</section></div>{creating&&<CreateTicket close={()=>setCreating(false)} done={id=>nav(routesPath.PROTECTED.SUPPORT.DETAIL(id))}/>}</main></DashboardLayout>}
function CreateTicket({close,done}:{close:()=>void;done:(id:string)=>void}){const[form,setForm]=useState({title:"",description:"",category:"SUPPORT",priority:"MEDIUM" as TicketPriority});const[create,{isLoading}]=useCreateTicketMutation();const submit=async(e:React.FormEvent)=>{e.preventDefault();const r=await create(form).unwrap();done(r.data.id)};return <div className="fixed inset-0 z-50 grid place-content-center bg-black/30 p-4"><form onSubmit={submit} className="w-full max-w-xl rounded-xl bg-white p-6 shadow-xl"><h2 className="text-xl font-semibold">Create support ticket</h2><p className="text-sm text-gray-01">Tell the support team what you need help with.</p><div className="mt-6 space-y-4"><label className="grid gap-1 text-sm font-medium">Title<Input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label className="grid gap-1 text-sm font-medium">Description<Textarea required rows={7} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><div className="grid grid-cols-2 gap-3"><label className="grid gap-1 text-sm font-medium">Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="h-9 rounded-md border px-3"><option>BUG</option><option>SUPPORT</option><option>HELP</option><option>ACCOUNT</option><option>BILLING</option><option>OTHER</option></select></label><label className="grid gap-1 text-sm font-medium">Priority<select value={form.priority} onChange={e=>setForm({...form,priority:e.target.value as TicketPriority})} className="h-9 rounded-md border px-3"><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>URGENT</option></select></label></div></div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={close} className="rounded-md border px-4 py-2 text-sm">Cancel</button><button disabled={isLoading} className="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white">{isLoading?"Creating…":"Create ticket"}</button></div></form></div>}

import { useState } from "react";import { Activity,Blocks,Download,FileKey2,Flag,Layers3,Loader2,Plus,Search,SlidersHorizontal } from "lucide-react";import DashboardLayout from "@/components/layout/dashboard-layout";import { Input } from "@/components/ui/input";import { cn } from "@/lib/utils";import { usePermissions } from "@/hooks/use-permissions";import { P,type PermissionCode } from "@/permissions";import { useArchiveCapabilityMutation,useArchiveConfigDefinitionMutation,useGetCapabilitiesQuery,useGetConfigAuditQuery,useGetConfigDefinitionsQuery,useGetConfigValuesQuery,useGetEntitlementsQuery,useGetOverridesQuery,useLazyExportConfigQuery } from "@/redux/services/config-api";import { ConfigDialog,type ConfigDialogMode } from "./config-dialog";
type Tab="values"|"definitions"|"capabilities"|"entitlements"|"overrides"|"audit";
const tabs:Array<{key:Tab;label:string;icon:typeof Layers3;permission:PermissionCode}>=[{key:"values",label:"Configuration values",icon:SlidersHorizontal,permission:P.VIEW_CONFIG_VALUES},{key:"definitions",label:"Definitions",icon:FileKey2,permission:P.VIEW_CONFIG_DEFINITIONS},{key:"capabilities",label:"Capabilities",icon:Blocks,permission:P.VIEW_CAPABILITIES},{key:"entitlements",label:"Entitlements",icon:Layers3,permission:P.VIEW_ENTITLEMENTS},{key:"overrides",label:"Overrides",icon:Flag,permission:P.VIEW_CONFIG_OVERRIDES},{key:"audit",label:"Audit trail",icon:Activity,permission:P.VIEW_CONFIG_AUDIT}];
export default function Settings(){const{hasPermission}=usePermissions();const visible=tabs.filter(t=>hasPermission(t.permission));const[tab,setTab]=useState<Tab>(visible[0]?.key??"values");const[exportConfig,{isFetching}]=useLazyExportConfigQuery();const download=async()=>{const result=await exportConfig().unwrap();const url=URL.createObjectURL(new Blob([JSON.stringify(result.data,null,2)],{type:"application/json"}));const anchor=document.createElement("a");anchor.href=url;anchor.download=`configuration-${new Date().toISOString().slice(0,10)}.json`;anchor.click();URL.revokeObjectURL(url)};return <DashboardLayout title="Settings"><main className="px-4.5 py-6 lg:px-8"><div className="mx-auto max-w-7xl space-y-6"><div className="flex items-end justify-between gap-4"><div><h1 className="text-2xl font-semibold">Platform settings</h1><p className="mt-1 text-sm text-gray-01">Control configuration, capabilities and scoped access from one place.</p></div>{hasPermission(P.EXPORT_CONFIG)&&<button disabled={isFetching} onClick={download} className="inline-flex items-center gap-2 rounded-md border bg-white px-3 py-2 text-sm font-medium"><Download className="size-4"/>{isFetching?"Exporting…":"Export snapshot"}</button>}</div><div className="grid gap-5 lg:grid-cols-[230px_minmax(0,1fr)]"><nav className="h-fit rounded-xl border bg-white p-2 shadow-sm">{visible.map(t=><button key={t.key} onClick={()=>setTab(t.key)} className={cn("flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm",tab===t.key?"bg-pry-01 font-semibold text-primary":"text-gray-600 hover:bg-gray-50")}><t.icon className="size-4"/>{t.label}</button>)}</nav><section className="min-w-0 overflow-hidden rounded-xl border bg-white shadow-sm">{tab==="definitions"&&<Definitions/>}{tab==="values"&&<Values/>}{tab==="capabilities"&&<Capabilities/>}{tab==="entitlements"&&<Entitlements/>}{tab==="overrides"&&<Overrides/>}{tab==="audit"&&<Audit/>}</section></div></div></main></DashboardLayout>}
function Header({title,description,add}:{title:string;description:string;add?:string}){const{hasPermission}=usePermissions();const[open,setOpen]=useState(false);const mode:ConfigDialogMode=add==="New definition"?"definition":add==="Set value"?"value":add==="New capability"?"capability":add==="Set entitlement"?"entitlement":"override";const required=mode==="definition"?P.CREATE_CONFIG_DEFINITION:mode==="value"?P.UPDATE_CONFIG_VALUES:mode==="capability"?P.MANAGE_CAPABILITIES:mode==="entitlement"?P.MANAGE_ENTITLEMENTS:P.MANAGE_CONFIG_OVERRIDES;const allowed=add&&hasPermission(required);return <><div className="flex items-center justify-between border-b p-5"><div><h2 className="font-semibold">{title}</h2><p className="text-xs text-gray-01">{description}</p></div>{allowed&&<button onClick={()=>setOpen(true)} className="inline-flex items-center gap-2 rounded-md bg-primary px-3 py-2 text-xs font-semibold text-white"><Plus className="size-4"/>{add}</button>}</div>{open&&<ConfigDialog mode={mode} close={()=>setOpen(false)}/>}</>}
function Definitions(){const q=useGetConfigDefinitionsQuery();const{hasPermission}=usePermissions();const[archive]=useArchiveConfigDefinitionMutation();const canArchive=hasPermission(P.ARCHIVE_CONFIG_DEFINITION);return <><Header title="Configuration definitions" description="The catalogue of typed settings and their validation rules." add="New definition"/><Data loading={q.isLoading} heads={["Setting","Type","Scopes","Sensitivity","Updated",...(canArchive?["Action"]:[])]} rows={(q.data?.data??[]).map(x=>[<div><p className="font-medium">{x.label}</p><code className="text-[11px] text-gray-01">{x.key}</code></div>,x.value_type,x.allowed_scopes.join(", "),x.sensitivity,new Date(x.updated_at).toLocaleDateString(),...(canArchive?[<button className="text-xs font-medium text-destructive" onClick={()=>{if(confirm(`Archive ${x.label}?`))archive({key:x.key,reason:"Archived from Settings console"})}}>Archive</button>]:[])])}/></>}
function Values(){const q=useGetConfigValuesQuery({});return <><Header title="Configuration values" description="Explicit values at platform, school and branch scope." add="Set value"/><div className="border-b p-4"><div className="relative max-w-sm"><Search className="absolute left-3 top-2.5 size-4 text-gray-01"/><Input className="pl-9" placeholder="Search configuration"/></div></div><Data loading={q.isLoading} heads={["Key","Value","Scope","Updated by","Updated"]} rows={(q.data?.data??[]).map(x=>[<code className="text-xs">{x.key}</code>,<code className="max-w-60 truncate text-xs">{JSON.stringify(x.value)}</code>,x.branch?"Branch":x.school?"School":"Platform",x.updated_by?.full_name??"System",new Date(x.updated_at).toLocaleDateString()])}/></>}
function Capabilities(){const q=useGetCapabilitiesQuery();const{hasPermission}=usePermissions();const[archive]=useArchiveCapabilityMutation();const canManage=hasPermission(P.MANAGE_CAPABILITIES);return <><Header title="Capabilities" description="Feature availability, limits and dependency rules." add="New capability"/><Data loading={q.isLoading} heads={["Capability","Kind","Default","Entitlement","Dependencies",...(canManage?["Action"]:[])]} rows={(q.data?.data??[]).map(x=>[<div><p className="font-medium">{x.label}</p><code className="text-[11px] text-gray-01">{x.key}</code></div>,x.kind,x.default_enabled?"Enabled":"Disabled",x.requires_entitlement?"Required":"No",x.dependencies.length||"—",...(canManage?[<button className="text-xs font-medium text-destructive" onClick={()=>{if(confirm(`Archive ${x.label}?`))archive({key:x.key,reason:"Archived from Settings console"})}}>Archive</button>]:[])])}/></>}
function Entitlements(){const q=useGetEntitlementsQuery({});return <><Header title="Entitlements" description="Platform and school grants that unlock capabilities." add="Set entitlement"/><Data loading={q.isLoading} heads={["Capability","School","State","Source","Period"]} rows={(q.data?.data??[]).map(x=>[x.capability_key,x.school??"Platform",x.state,x.source,x.starts_at?new Date(x.starts_at).toLocaleDateString():"Always"])}/></>}
function Overrides(){const q=useGetOverridesQuery({});return <><Header title="Overrides" description="Explicit school and branch capability decisions." add="Add override"/><Data loading={q.isLoading} heads={["Capability","Scope","State","Reason","Updated"]} rows={(q.data?.data??[]).map(x=>[x.capability_key,x.branch?"Branch":x.school?"School":"Platform",x.state,x.reason||"—",new Date(x.updated_at).toLocaleDateString()])}/></>}
function Audit(){const[createdAfter]=useState(()=>new Date(Date.now()-30*864e5).toISOString());const q=useGetConfigAuditQuery({created_after:createdAfter});return <><Header title="Configuration audit trail" description="Immutable changes from the last 30 days."/><Data loading={q.isLoading} heads={["Action","Target","Actor","Reason","Date"]} rows={(q.data?.data??[]).map(x=>[x.action,`${x.target_type} · ${x.target_id}`,x.actor?.full_name??"System",x.reason||"—",new Date(x.created_at).toLocaleString()])}/></>}
function Data({loading,heads,rows}:{loading:boolean;heads:string[];rows:React.ReactNode[][]}){if(loading)return <div className="grid h-60 place-content-center"><Loader2 className="animate-spin text-primary"/></div>;return <div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-white-05 text-left text-xs text-gray-01"><tr>{heads.map(h=><th className="p-3 font-medium" key={h}>{h}</th>)}</tr></thead><tbody className="divide-y">{rows.map((r,i)=><tr className="hover:bg-gray-50" key={i}>{r.map((c,j)=><td className="p-3" key={j}>{c}</td>)}</tr>)}</tbody></table>{!rows.length&&<div className="py-16 text-center text-sm text-gray-01">No records found.</div>}</div>}

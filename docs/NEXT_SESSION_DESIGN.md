# Next session - Procurement **Analytics** rebuild (AP Aging · GR/IR & Control · Spend · Vendor Performance)

Continue rebuilding the Procurement console from `Procurement_Console.html`, working top-to-bottom by navigation. **Analytics is the sixth and final navigation group - finishing it completes the Procurement console rebuild.** Start by reading these completely:
- `docs/PROCUREMENT_BUILD_NOTES.md` - the source of truth and living handoff document (see §6 roadmap + the Inventory completion note for the current bar).
- `AGENTS.md`
- The relevant existing frontend and backend Procurement report code.
- The prototype implementation and every state for each Analytics screen.

Repositories:
- Frontend: `/Users/mac/Documents/Dev-Projects/GitHub/console-fe`
- Backend: `/Users/mac/Documents/Dev-Projects/GitHub/backend`

Execution model - agent roles:
- Run this session on Fable as the designer and orchestrator. Fable owns: studying the prototype, resolving all design and architecture decisions, writing the implementation plan, getting my sign-off, reviewing implemented code, driving verification (verify-design, the 390px/820px overflow audit, screenshot inspection), maintaining `docs/PROCUREMENT_BUILD_NOTES.md`, and handling commits after my approval.
- Before deploying any code agent, Fable verifies that an Opus 4.8 agent type with high reasoning effort is defined (`.claude/agents/*.md` frontmatter, or the SDK `agents` map). One already exists at `.claude/agents/opus-implementer.md` (`model: claude-opus-4-8`, `effort: high`). Reuse it; if it is missing, recreate it (Opus 4.8, high reasoning effort, code-editing tools) before deploying - so the code work runs at the intended model AND effort, not just a bare model override that drops the reasoning setting.
- Fable does NOT write the production code itself. After I approve the plan, Fable deploys that Opus 4.8 high-reasoning agent to implement the section (frontend and backend) per the approved plan and Fable's design spec. The Opus agent runs the build, tests, and lint and returns its results; Fable reviews the diff, checks it against the plan and the design bar, requests fixes, and re-deploys the agent as needed until the section meets the bar. (Note: the code agent may hit session limits mid-run; if it does, Fable assesses the partial state and either finishes the increment itself or resumes the agent once limits reset - never leaving the tree half-wired. A small orchestrator-owned design fix - e.g. a mislabel found in review - may be handed back to the same agent via a resume message rather than re-writing it in-session.)
- Keep the design/orchestration/verification loop with Fable; keep code implementation with the Opus 4.8 high agent. Do not collapse the two roles. Hand the Opus agent enough context (files, contracts, decisions, design intent) to work without this chat history.

Current completed and verified baseline (all rebuilt to the prototype):
- Dashboard · Requisitions · Purchase Orders · Goods Receipts · Vendor Invoices · Vendor Payments · Approvals · Vendors · Categories · Catalog · RFQs · Quotations · Contracts · **Stock Items · Movements** (Inventory group now complete). **Analytics (§6) is the only remaining group.**

Latest commits:
- Frontend: `26ab970` - `feat(procurement): rebuild Inventory (Stock Items + Movements) with live posting recaps`
- Backend: `692ac61` - `feat(procurement): inventory stock console - items, movements, issue/adjust + seed`

Repo state:
- **Both worktrees are clean.** Migrations are applied through `vs_procurement/0013_purchaseorder_contract`; the Inventory work added **no migration** (`makemigrations --check` is clean). If any unrelated change reappears in the backend worktree (e.g. the old `vs_audit` `school`→`tenant` rename), keep it out of your commits.
- The full Procurement backend suite is green (**162 tests**). Do not introduce a test gap for Analytics.

Continue with the next unchecked navigation section in `docs/PROCUREMENT_BUILD_NOTES.md` - the **Analytics** group (§6). Build it top-to-bottom by nav: **AP Aging**, then **GR/IR & Control**, then **Spend**, then **Vendor Performance** - but in your plan, decide and justify whether they ship as one section/commit or in batches, and confirm each stays its own route/page (today all four share `analytics.tsx` via the `:section` route, exactly like Inventory shared `inventory.tsx`).

Important current context:
- Analytics is the sixth navigation group. Its leaves are **AP Aging** (`/procurement/analytics/ap-aging`), **GR/IR & Control** (`/procurement/analytics/grir`), **Spend** (`/procurement/analytics/spend`), and **Vendor Performance** (`/procurement/analytics/performance`), all nav-gated on the `procurement.report.` prefix. Router: `analytics.tsx` on `${P.ANALYTICS}` and `${P.ANALYTICS}/:section`.
- **All Analytics screens are READ-ONLY report aggregates** - no create/edit/post, no drawers that mutate, **no Export controls anywhere**. The one hard rule from every prior section applies with full force here: an "honest adaptation" only justifies dropping *fabricated* data (invented quality grades, responsiveness scores, on-time %, price-variance, SLA metrics the backend does not actually compute) - it must NOT be used to silently drop a real, user-facing prototype concept the model merely lacks. Where a real prototype concept lacks backing, extend the backend or surface it to me as an explicit decision (as we did with RFQ invited-vendors and the PO→contract call-off link). **Vendor Performance is the highest-risk screen for this** - verify exactly what `reports/vendor-performance/` computes before designing it, and do not render any score/grade the endpoint doesn't return.
- Permission: everything gates on `procurement.report.view` (FE `P.PROC_VIEW_PROC_REPORTS` 701401). No new permission keys are expected; if you add a report endpoint, gate it on `procurement.report.view` and note it in `PERMISSIONS_AUDIT.md`.
- Backend report endpoints (all entity-scoped, all `procurement.report.view`, `vs_procurement/urls.py` L111–121): `reports/dashboard/`, `ap-aging/`, `ap-reconciliation/`, `grir/`, `ap-cash-requirements/`, `grir-aging/`, `spend-analysis/`, `vendor-performance/`, `cycle-time/`, `stock-reorder/`, `stock-valuation/`. **This effort is a FE redesign** - the report services already exist (`vs_procurement/reports.py`). Confirm each response contract before building; do not assume the stub's shape is complete.
- FE hooks already wired in `procurement-ext-api.ts`: `getApAging`, `getApReconciliation`, `getGrirBalance`, `getSpendAnalysis`, `getVendorPerformance`, `getStockReorderReport`, `getStockValuationReport`. **Missing hooks** to add if the prototype surfaces them: `grir-aging`, `ap-cash-requirements`, `cycle-time`.
- The existing `src/pages/protected/procurement/analytics.tsx` is a ~136-line **pre-prototype** tabbed stub (four basic tables: AP Aging / GR/IR / Spend / Vendor Performance) with no KPI strips, charts, filters, period controls, aging visuals or per-screen layout - treat it like the old Inventory/Catalog/Sourcing stubs and rebuild in place (routes unchanged; keep the `:section` split).
- Reuse the finance-ui **charts** primitives (already used by the §1 Dashboard - study it as the in-house reference): `BarChart`, `Donut`, `StatStrip`, `Sparkline`, `BudgetBar`, `AgingStack`, `TrendArea`, `CHART_COLORS` from `@/components/finance-ui`. Money is integer **kobo** on the wire, formatted in the UI (`Money`/`formatMoney`).
- **Responsive:** dense report tables opt out of phone cards with `mobile="scroll"` (as the current analytics tables and the Movements ledger do); KPI strips `grid-cols-2 … lg:grid-cols-4` (money KPIs 1-col on phones); charts must not force page overflow (wrap wide charts in `overflow-x-auto`). Every changed screen must work on desktop, tablet, and phone without horizontal overflow. Guard every top-level list/`rows` payload with `toArray()` - an empty list serialises as `{}` and a raw `.map` crashes the screen; drive and screenshot the **empty** state, not just the populated one.

Resolve these decisions before implementation:
- Ship-together vs batches; confirm four separate routed pages (or a shared page per `:section`), and where a period/date-range or as-of filter belongs on each (AP Aging as-of date; Spend start/end; Vendor Performance period).
- Each screen's authoritative aggregate source and exact response contract (buckets, totals, per-vendor/per-category rows, GR/IR clearing balance + aging, spend by vendor/category/period, vendor on-time/price-variance/spend). **Never invent** aging buckets, turnover, on-time %, quality grades, or variance the endpoint does not compute - prefer the secure server aggregates and surface any real-but-unbacked prototype concept to me.
- Whether GR/IR & Control needs both `reports/grir/` (balance) and `reports/grir-aging/` (goods-received-not-invoiced / invoiced-not-received aging), and whether AP Aging should also surface `ap-cash-requirements/` - add the missing FE hooks only for what a screen actually renders.
- KPI strips per screen (e.g. total AP outstanding, GR/IR net, total spend, active-vendor spend) - derive from the report payload where possible; only add a small entity-scoped summary endpoint if the prototype needs a figure no existing report returns, gated `procurement.report.view`.
- Chart choices per screen (aging stack, spend donut/bars, trend line) mapped to real fields; honest empty states when a period has no data.
- Permission model (all `procurement.report.view`), entity isolation, and read-only guarantees (no mutation anywhere in Analytics).

Required workflow (unchanged): do not start building immediately. (1) Fable renders and thoroughly studies each Analytics prototype screen - AP Aging, GR/IR & Control, Spend, Vendor Performance - and every meaningful state: populated report, empty/new-entity, each filter/period control, KPI strips, every chart, loading/error/forbidden, and any drill-down. (2) Compare each prototype screen against the existing `analytics.tsx`, the `vs_procurement/reports.py` services + their view/serializer contracts, the `procurement.report.view` gating, the entity resolver, the finance-ui charts primitives, and real seeded CODEX data. (3) Resolve the decisions above. (4) Present a concrete plan for my sign-off before any code. (5) Only after I approve, deploy the Opus 4.8 high agent; review its diff against the plan and the design bar; re-deploy for fixes; then run verification yourself.

Design intent - improve on the prototype, don't just copy it (the prototype is the floor, not the ceiling), as every prior section did (KPI strips, real charts, honest empty/restricted states, period/as-of filters, dense responsive tables). Call out each deliberate improvement in the plan. Match the prototype's spacing, columns, KPIs, charts, filters and empty states as the baseline, then refine. Use the house theme and shared finance-ui primitives: `DataTable`/dense tables with `mobile="scroll"`, the `charts` kit, `KpiCard`/`StatStrip`, `Money`/`formatMoney`. Real backend data only. No Export buttons. No mutations.

Backend inline-comment requirement: if you touch any backend report file, review it (not only new lines) and add concise inline comments beside non-obvious joins, aggregates, entity checks, bucket math, and period derivation. Keep ordinary code uncluttered. (This section is expected to be mostly FE; only extend the backend where a real prototype concept lacks backing, with my sign-off.)

Documentation requirement: update `docs/PROCUREMENT_BUILD_NOTES.md` after every material decision or behavior change (mark §6 done and record each deliberate improvement), and update `PERMISSIONS_AUDIT.md` for any new/changed gating. **When Analytics is complete, note in the build notes that the full Procurement console rebuild is finished.**

Verification before completion (Fable owns this loop, not the code agent):
- Frontend production build (`tsc -b`/`npm run build` is the real gate), frontend tests, and changed-file lint.
- Focused and full Procurement backend tests with `--keepdb`; Django checks; migration-drift check (`makemigrations --check`) - expected clean/no migration for a FE-only redesign; if you extend the backend, add security-first tests (403 on `procurement.report.view` denial; entity isolation; aggregate correctness) and keep the suite green.
- `verify-design` against the real backend for routes `/procurement/analytics/ap-aging`, `/grir`, `/spend`, `/performance`; ensure CODEX has data these reports aggregate (`seed_finance_ar_demo --all` + `seed_procurement_demo` give posted invoices, GRNs, payments, POs and stock - extend the seed idempotently only if a report renders empty and a real fixture is missing). Verify genuinely populated reports and every filter/period/chart/empty state, inspect all screenshots, run the 390px/820px overflow audit and inspect those too, then scrub verifier-created login/audit rows. Confirm both repos contain only intended changes.

Git workflow: keep frontend and backend commits separate; do not amend or rewrite completed baseline commits; commit only after I approve the completed section (Fable performs the commits); end commit bodies with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

Begin (as Fable) by rendering and studying each Analytics prototype screen (AP Aging, GR/IR & Control, Spend, Vendor Performance) and inspecting the existing `analytics.tsx`, the `vs_procurement/reports.py` services and their view/serializer contracts, the report permission gating, the finance-ui charts primitives, and real seeded CODEX data. Resolve the per-screen aggregate sources, filters/period controls, KPI/chart choices, honest-adaptation calls (especially Vendor Performance), and the read-only/permission model. Present the plan for my approval, then deploy the Opus 4.8 high agent to implement it - and when it lands, record that the Procurement console rebuild is complete.
